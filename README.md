# ST-SoftGreedy
We introduce ST-SoftGreedy, a policy output layer whose forward pass runs an exact greedy algorithm over a partition matroid intersected with a knapsack, so that emitted actions are always feasible and integral, and whose backward pass differentiates a smooth relaxation built from a corrected soft ranking operator and margin-gated sigmoids
